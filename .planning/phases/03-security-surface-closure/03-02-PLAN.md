---
phase: 03-security-surface-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/user.py
  - app/api/routes/users.py
  - tests/test_api_routes.py
  - tests/test_phase6_unit.py
autonomous: true
requirements:
  - SEC-02
user_setup: []
must_haves:
  truths:
    - kitchen config rejects burner counts above `10` and user equipment counts above `20`
    - cross-field validation rejects `len(burners) > max_burners`
    - second-oven rack values are rejected when `has_second_oven` is false, with explicit validation errors instead of silent normalization
  artifacts:
    - app/models/user.py
    - app/api/routes/users.py
    - tests/test_api_routes.py
  key_links:
    - tests/test_api_routes.py exercises kitchen update and equipment entry points where invalid profile data must now be rejected
    - tests/test_phase6_unit.py continues to pin scheduler-facing kitchen-config behavior after the stricter validation lands
---

<objective>
Close the kitchen-profile validation gap by enforcing the locked numeric ceilings and cross-field invariants before invalid profile data can reach scheduling logic.

Purpose: Phase 3 cannot claim secure input handling if structurally impossible kitchen data is still accepted and silently passed into the scheduler.
Output: stricter kitchen config and equipment validation plus route/scheduler regressions proving invalid relational data is rejected, not normalized.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-security-surface-closure/03-CONTEXT.md
@.planning/phases/03-security-surface-closure/03-RESEARCH.md
@.planning/phases/03-security-surface-closure/03-VALIDATION.md
@app/models/user.py
@app/api/routes/users.py
@tests/test_api_routes.py
@tests/test_phase6_unit.py
</context>

<threat_model>
## Trust Boundaries

- Kitchen profile data is user-controlled and later influences scheduler resource constraints.
- Equipment records unlock recipe-technique paths and need a bounded cardinality guard.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | kitchen config bounds | mitigate | Enforce burner/rack ceilings and max equipment count before invalid data can persist or reach scheduling. |
| T-03-04 | Tampering | kitchen config relational invariants | mitigate | Reject impossible burner and second-oven combinations with explicit validation errors instead of normalizing server-side. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add numeric ceilings for kitchen and equipment inputs</name>
  <files>app/models/user.py, app/api/routes/users.py, tests/test_api_routes.py</files>
  <read_first>app/models/user.py, app/api/routes/users.py, tests/test_api_routes.py, .planning/codebase/CONCERNS.md, .planning/phases/03-security-surface-closure/03-CONTEXT.md</read_first>
  <action>Implement the locked numeric ceilings: `max_burners <= 10` and maximum equipment count per user `<= 20`, using the existing validation patterns in the user/kitchen models and routes. Add or extend route tests so over-limit kitchen and equipment submissions return validation or domain errors before scheduling code runs.</action>
  <verify>pytest -q tests/test_api_routes.py -o addopts='' -k 'kitchen or equipment or burner'</verify>
  <acceptance_criteria>
    - kitchen burner count above `10` is rejected
    - adding equipment above the `20` item cap is rejected
    - `tests/test_api_routes.py` covers both limit failures
  </acceptance_criteria>
  <done>The route/model layer now enforces the selected numeric ceilings for kitchen and equipment data.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce cross-field invariants without silent normalization</name>
  <files>app/models/user.py, app/api/routes/users.py, tests/test_api_routes.py, tests/test_phase6_unit.py</files>
  <read_first>app/models/user.py, app/api/routes/users.py, tests/test_phase6_unit.py, tests/test_api_routes.py, .planning/phases/03-security-surface-closure/03-RESEARCH.md</read_first>
  <action>Add cross-field validation that rejects `len(burners) > max_burners` and rejects second-oven rack values when `has_second_oven` is false. Extend route and scheduler-facing regressions so Phase 3 proves invalid relational data is rejected rather than normalized or truncated.</action>
  <verify>pytest -q tests/test_api_routes.py tests/test_phase6_unit.py -o addopts='' -k 'kitchen or burner'</verify>
  <acceptance_criteria>
    - invalid burner-list cardinality is rejected with a validation error
    - second-oven rack fields are rejected when no second oven is enabled
    - no server-side normalization/truncation path is introduced
  </acceptance_criteria>
  <done>Kitchen config now enforces the locked relational invariants, and regressions prove invalid structures are rejected outright.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_api_routes.py tests/test_phase6_unit.py -o addopts='' -k 'kitchen or burner'` passes
- [ ] kitchen input above the selected ceilings is rejected
- [ ] invalid relational kitchen data is rejected instead of normalized
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- kitchen config and equipment limits now reject out-of-policy inputs
- cross-field kitchen invariants are enforced before scheduling

</success_criteria>

<output>
After completion, create `.planning/phases/03-security-surface-closure/03-02-SUMMARY.md`
</output>
