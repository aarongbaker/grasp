---
phase: 01-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - tests/test_api_routes.py
autonomous: true
requirements:
  - TEST-02
  - TEST-05
user_setup: []
must_haves:
  truths:
    - tests/test_api_routes.py covers GET /api/v1/health for both the connected path and the degraded DB-failure path
    - tests/test_api_routes.py covers POST and DELETE equipment routes for success and denial/error contracts
    - the route-test mock session can persist and delete Equipment rows by equipment_id without requiring a real DB
  artifacts:
    - tests/test_api_routes.py
  key_links:
    - tests/test_api_routes.py drives app/api/routes/health.py through FastAPI routing
    - tests/test_api_routes.py drives app/api/routes/users.py equipment handlers through FastAPI routing
---

<objective>
Add HTTP-level contract coverage for the untested Phase 1 routes: `/api/v1/health` and the equipment CRUD endpoints under `/api/v1/users/{user_id}/equipment`.

Purpose: these route contracts are production entry points today, and later hardening phases depend on their authorization and persistence behavior being locked down by tests.
Output: expanded `tests/test_api_routes.py` coverage for health and equipment route behavior, plus any mock-session support needed for those tests.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@tests/test_api_routes.py
@app/api/routes/health.py
@app/api/routes/users.py
@app/models/user.py
</context>

<threat_model>
## Trust Boundaries

- Route tests must exercise FastAPI routing and auth/ownership checks, not direct function calls.
- Mock DB behavior must match the route code closely enough that false-positive 2xx assertions are avoided.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | equipment route auth coverage | mitigate | Keep the existing `get_current_user` override path and add explicit cross-user 403 assertions for equipment create/delete. |
| T-01-04 | Denial of Service | health route degraded-path test | mitigate | Use `ASGITransport(..., raise_app_exceptions=False)` so the test asserts the real 500 contract when `db.execute()` fails instead of swallowing the exception. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend MockDBSession so equipment routes can be tested end-to-end</name>
  <files>tests/test_api_routes.py</files>
  <read_first>tests/test_api_routes.py, app/api/routes/users.py, app/models/user.py</read_first>
  <action>Update `MockDBSession` inside `tests/test_api_routes.py` so it can store `Equipment` rows keyed by `equipment_id`, return them from `exec(select(Equipment)...where(...))`, and remove them through an `async def delete()` implementation. Preserve the existing authored-recipe and cookbook query behavior while adding the minimum additional logic required for the equipment route handlers to behave like the real session.</action>
  <verify>pytest -q tests/test_api_routes.py -o addopts='' -k 'equipment'</verify>
  <acceptance_criteria>
    - `tests/test_api_routes.py` contains `async def delete(` on `MockDBSession`
    - `tests/test_api_routes.py` references `Equipment` when seeding or querying route-test rows
    - equipment route tests can assert row presence before delete and absence after delete
  </acceptance_criteria>
  <done>The route-test mock session supports equipment create/delete flows without a real database.</done>
</task>

<task type="auto">
  <name>Task 2: Add health and equipment route contract tests</name>
  <files>tests/test_api_routes.py</files>
  <read_first>tests/test_api_routes.py, app/api/routes/health.py, app/api/routes/users.py, app/core/auth.py</read_first>
  <action>Add new async tests to `tests/test_api_routes.py` with these exact contracts: `GET /api/v1/health` returns HTTP 200 and `{\"status\": \"ok\", \"db\": \"connected\"}` when `db.execute()` succeeds; `GET /api/v1/health` returns HTTP 500 when `db.execute()` raises and the ASGI transport is configured with `raise_app_exceptions=False`; `POST /api/v1/users/{user_id}/equipment` returns HTTP 201 with `equipment_id`, `user_id`, `name`, `category`, and `unlocks_techniques`; `DELETE /api/v1/users/{user_id}/equipment/{equipment_id}` returns HTTP 204 for the owner, HTTP 403 for a cross-user caller, and HTTP 404 when the equipment row is missing.</action>
  <verify>pytest -q tests/test_api_routes.py -o addopts='' -k 'health or equipment'</verify>
  <acceptance_criteria>
    - `tests/test_api_routes.py` contains `async def test_health_check_returns_ok_with_connected_db(`
    - `tests/test_api_routes.py` contains `async def test_health_check_returns_500_when_db_execute_fails(`
    - `tests/test_api_routes.py` contains `async def test_add_equipment_returns_201_and_persists_row(`
    - `tests/test_api_routes.py` contains `async def test_delete_equipment_returns_204_and_removes_row(`
    - `tests/test_api_routes.py` contains 403 and 404 equipment route assertions
  </acceptance_criteria>
  <done>Health and equipment endpoints have explicit HTTP-contract coverage for both success and failure/denial paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_api_routes.py -o addopts='' -k 'health or equipment'` passes
- [ ] the health degraded-path test asserts an HTTP 500 response rather than leaking the underlying exception to the client
- [ ] equipment create/delete tests assert both HTTP status codes and row persistence/removal behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `/health` success and failure behavior are covered
- equipment CRUD route success, 403, and 404 contracts are covered

</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-02-SUMMARY.md`
</output>

