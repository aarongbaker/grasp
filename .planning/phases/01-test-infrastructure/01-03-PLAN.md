---
phase: 01-test-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - tests/test_ingestion_tasks.py
  - tests/test_pipeline_tasks.py
autonomous: true
requirements:
  - TEST-03
  - TEST-04
user_setup: []
must_haves:
  truths:
    - tests/test_pipeline_tasks.py covers the missing-session, missing-user, ValidationError, and graph-exception branches of _run_pipeline_async
    - tests/test_ingestion_tasks.py remains green after pytest ignore removal
    - _run_pipeline_async tests assert finalise_session is skipped on early returns and called with FAILED/unknown payloads on failure paths
  artifacts:
    - tests/test_pipeline_tasks.py
    - tests/test_ingestion_tasks.py
  key_links:
    - tests/test_pipeline_tasks.py patches app/workers/tasks.py dependencies at the same import points _run_pipeline_async uses
    - tests/test_ingestion_tasks.py continues to validate _ingest_async status transitions and error propagation
---

<objective>
Add the missing Celery worker task failure-path coverage by introducing `_run_pipeline_async` tests and keeping the existing `_ingest_async` suite active after ignore removal.

Purpose: Phase 2 correctness fixes rely on task-level regression coverage for finalisation and failure handling before changing worker behavior.
Output: a dedicated `tests/test_pipeline_tasks.py` suite plus any maintenance required in `tests/test_ingestion_tasks.py` so both task modules run under normal pytest discovery.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@tests/test_ingestion_tasks.py
@app/workers/tasks.py
@app/core/status.py
</context>

<threat_model>
## Trust Boundaries

- Worker-task tests must validate the production error-classification contract without requiring a broker or real LangGraph/Postgres services.
- Early-return branches are security-relevant because they should not emit misleading terminal writes.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Repudiation | session finalisation in task wrapper | mitigate | Assert that missing-session and missing-user branches do not call `finalise_session()`, while ValidationError and graph-exception branches do. |
| T-01-06 | Tampering | failure payload shape | mitigate | Assert `errors[0].error_type == "unknown"` for unhandled graph exceptions and `FAILED` finalisation for validation failures. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated _run_pipeline_async failure-path tests</name>
  <files>tests/test_pipeline_tasks.py</files>
  <read_first>app/workers/tasks.py, tests/test_ingestion_tasks.py, app/core/status.py</read_first>
  <action>Create `tests/test_pipeline_tasks.py` with async unit tests that patch the dependency imports used inside `_run_pipeline_async()`: fake `AsyncPostgresSaver`, fake `build_grasp_graph()`, fake `create_async_engine()` / `sessionmaker()`, and a spy for `finalise_session()`. Implement four test cases with these exact assertions: missing session returns `None` and never calls `finalise_session`; missing user returns `None` and never calls `finalise_session`; `build_session_initial_state()` raising `ValidationError` calls `finalise_session()` once with a payload containing a validation error and `schedule` set to `None`; `graph.ainvoke()` raising an exception calls `finalise_session()` once with `errors[0][\"error_type\"] == \"unknown\"`.</action>
  <verify>pytest -q tests/test_pipeline_tasks.py -o addopts=''</verify>
  <acceptance_criteria>
    - `tests/test_pipeline_tasks.py` contains `async def test_run_pipeline_async_missing_session_returns_without_finalise(`
    - `tests/test_pipeline_tasks.py` contains `async def test_run_pipeline_async_missing_user_returns_without_finalise(`
    - `tests/test_pipeline_tasks.py` contains `async def test_run_pipeline_async_validation_error_finalises_failed_session(`
    - `tests/test_pipeline_tasks.py` contains `async def test_run_pipeline_async_graph_exception_records_unknown_error(`
  </acceptance_criteria>
  <done>The `_run_pipeline_async` failure branches are covered without a broker or real worker process.</done>
</task>

<task type="auto">
  <name>Task 2: Keep _ingest_async coverage active after ignore removal</name>
  <files>tests/test_ingestion_tasks.py</files>
  <read_first>tests/test_ingestion_tasks.py, app/workers/tasks.py</read_first>
  <action>Adjust `tests/test_ingestion_tasks.py` only as needed so its existing `_ingest_async` contract tests continue to run under normal discovery after Phase 1 removes pytest ignore rules. Preserve the current assertions around `IngestionStatus.COMPLETE`, `IngestionStatus.FAILED`, progress phases, rollback recovery, and embedded chunk counts; do not weaken those assertions to make the suite pass.</action>
  <verify>pytest -q tests/test_ingestion_tasks.py tests/test_pipeline_tasks.py -o addopts=''</verify>
  <acceptance_criteria>
    - `tests/test_ingestion_tasks.py` still contains `test_ingest_async_records_progress_and_completion`
    - `tests/test_ingestion_tasks.py` still contains `test_ingest_async_marks_failure_with_phase`
    - `tests/test_ingestion_tasks.py` still contains `test_ingest_async_recovers_after_commit_failure_and_marks_job_failed`
    - the targeted pytest command runs both task suites together
  </acceptance_criteria>
  <done>Both Celery task test files run together and preserve the task-failure contracts Phase 2 depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_ingestion_tasks.py tests/test_pipeline_tasks.py -o addopts=''` passes
- [ ] `_run_pipeline_async` tests assert both no-finalise early exits and failure finalisation payload shape
- [ ] `_ingest_async` coverage remains intact after ignore removal
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `_run_pipeline_async` has dedicated failure-path coverage
- `_ingest_async` remains covered and discoverable

</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-03-SUMMARY.md`
</output>

