---
phase: 01-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/conftest.py
  - tests/test_admin_invites.py
  - tests/test_invite_gating.py
  - tests/test_state_machine.py
  - tests/test_deploy_readiness.py
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - pytest.ini no longer contains --ignore entries for test_admin_invites.py, test_invite_gating.py, test_ingestion_tasks.py, test_state_machine.py, test_api_routes.py, or test_deploy_readiness.py
    - tests/conftest.py exposes reusable admin/invite auth fixtures so admin test suites do not redefine admin_user or _access_token_for locally
    - the previously ignored admin/invite, state-machine, and deploy-readiness suites collect from normal pytest discovery instead of being globally hidden
  artifacts:
    - pytest.ini
    - tests/conftest.py
    - tests/test_admin_invites.py
    - tests/test_invite_gating.py
  key_links:
    - tests/test_admin_invites.py imports or consumes fixtures from tests/conftest.py
    - tests/test_invite_gating.py imports or consumes fixtures from tests/conftest.py
---

<objective>
Re-open the ignored Phase 1 suites by moving shared route/auth test infrastructure into `tests/conftest.py`, deleting permanent pytest ignore rules, and stabilizing the already-written test files that should participate in normal collection.

Purpose: Phase 1 cannot execute in parallel later unless the shared harness and ignored-suite entry point are fixed first.
Output: shared fixtures in `tests/conftest.py`, cleaned `pytest.ini`, and admin/invite contract suites ready for normal collection.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@pytest.ini
@tests/conftest.py
@tests/test_admin_invites.py
@tests/test_invite_gating.py
@tests/test_state_machine.py
@tests/test_deploy_readiness.py
@app/api/routes/admin.py
@app/core/auth.py
</context>

<threat_model>
## Trust Boundaries

- Test fixtures that mint JWTs must still exercise the real auth dependency path.
- Global pytest config controls whether critical suites are visible to CI at all.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | shared auth fixtures | mitigate | Keep real `_build_access_token()`-backed tokens and only patch `get_settings` / DB overrides; do not stub `ensure_admin_user()` directly. |
| T-01-02 | Tampering | pytest discovery config | mitigate | Remove permanent `--ignore` entries from `pytest.ini` and verify the previously hidden suites run through explicit pytest commands. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared admin/invite fixtures into tests/conftest.py</name>
  <files>tests/conftest.py, tests/test_admin_invites.py, tests/test_invite_gating.py</files>
  <read_first>tests/conftest.py, tests/test_admin_invites.py, tests/test_invite_gating.py, app/api/routes/admin.py, app/core/auth.py</read_first>
  <action>Move the duplicated admin-route test harness into `tests/conftest.py`: create reusable fixtures/helpers for an admin user row, a non-admin user row, a `Settings(admin_email=...)` test configuration, and a real JWT builder backed by `app.api.routes.auth._build_access_token()`. Update `tests/test_admin_invites.py` and `tests/test_invite_gating.py` to consume those shared fixtures instead of defining their own `admin_user`, `non_admin_user`, or `_access_token_for` helpers, while preserving route-level app creation and `get_session` dependency overrides inside each test file.</action>
  <verify>pytest -q tests/test_admin_invites.py tests/test_invite_gating.py -o addopts=''</verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains reusable fixtures or helpers for admin-route auth setup
    - `tests/test_admin_invites.py` no longer defines `async def admin_user(`
    - `tests/test_invite_gating.py` no longer defines `async def admin_user(`
    - `tests/test_admin_invites.py` and `tests/test_invite_gating.py` still reference real bearer tokens, not direct `ensure_admin_user()` stubs
  </acceptance_criteria>
  <done>Shared admin/invite fixtures exist in `tests/conftest.py` and both suites use them successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Remove permanent ignore entries and stabilize the newly visible suites</name>
  <files>pytest.ini, tests/test_admin_invites.py, tests/test_invite_gating.py, tests/test_state_machine.py, tests/test_deploy_readiness.py</files>
  <read_first>pytest.ini, tests/test_admin_invites.py, tests/test_invite_gating.py, tests/test_state_machine.py, tests/test_deploy_readiness.py</read_first>
  <action>Delete the entire `addopts = --ignore=...` exclusion list from `pytest.ini` so the Phase 1 suites participate in normal discovery. After removing the ignores, fix only test-side assumptions in `tests/test_admin_invites.py`, `tests/test_invite_gating.py`, `tests/test_state_machine.py`, and `tests/test_deploy_readiness.py` that prevent collection or green execution against the current codebase. Do not reintroduce file-level ignores or convert these suites to skips just to avoid fixing them.</action>
  <verify>pytest -q tests/test_admin_invites.py tests/test_invite_gating.py tests/test_state_machine.py tests/test_deploy_readiness.py -o addopts=''</verify>
  <acceptance_criteria>
    - `pytest.ini` does not contain `--ignore=tests/test_admin_invites.py`
    - `pytest.ini` does not contain `--ignore=tests/test_api_routes.py`
    - `pytest.ini` does not contain any `--ignore=tests/test_*.py` entries
    - `tests/test_state_machine.py` and `tests/test_deploy_readiness.py` remain ordinary collected test modules, not globally ignored
  </acceptance_criteria>
  <done>Pytest no longer hides the Phase 1 suites through global ignore configuration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_admin_invites.py tests/test_invite_gating.py tests/test_state_machine.py tests/test_deploy_readiness.py -o addopts=''` passes
- [ ] `pytest --collect-only -q tests/test_admin_invites.py tests/test_invite_gating.py tests/test_state_machine.py tests/test_deploy_readiness.py -o addopts=''` succeeds
- [ ] `pytest.ini` has no permanent file ignore entries for the Phase 1 suites
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared admin/invite fixture setup is centralized in `tests/conftest.py`
- Previously ignored Phase 1 suites are discoverable without `pytest.ini` exclusions

</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-01-SUMMARY.md`
</output>

