---
phase: 01-test-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - tests/test_kitchen_edge_cases.py
  - tests/test_phase6_unit.py
autonomous: true
requirements:
  - TEST-05
  - TEST-06
user_setup: []
must_haves:
  truths:
    - tests/test_kitchen_edge_cases.py exercises _merge_dags directly for zero burners, missing kitchen config, and malformed burner-descriptor inputs
    - scheduler regression tests prove equipment constraints disappear when the relevant equipment is absent from kitchen_config
    - the new kitchen edge suite is suitable as a regression gate for Phase 4 performance work
  artifacts:
    - tests/test_kitchen_edge_cases.py
    - tests/test_phase6_unit.py
  key_links:
    - tests/test_kitchen_edge_cases.py calls app/graph/nodes/dag_merger._merge_dags directly, matching the Phase 6 unit-test style
    - tests/test_phase6_unit.py contains the equipment-constraint regression that Phase 4 will reuse
---

<objective>
Create the scheduler edge-case regression suite that Phase 4 will rely on, including kitchen configuration failures and equipment-constraint unlock behavior.

Purpose: performance work later in the milestone must be gated by concrete scheduler regression coverage, not by assumptions about the current `_merge_dags()` behavior.
Output: a dedicated `tests/test_kitchen_edge_cases.py` suite plus any targeted scheduler regression additions needed in `tests/test_phase6_unit.py`.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@tests/test_phase6_unit.py
@app/graph/nodes/dag_merger.py
@app/models/user.py
@tests/fixtures/recipes.py
</context>

<threat_model>
## Trust Boundaries

- Scheduler regression tests must interact with the pure `_merge_dags()` boundary, not the graph wrapper, so failures map directly to scheduling math.
- Edge-case tests should lock down fail-safe behavior instead of asserting brittle full-schedule snapshots.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | malformed kitchen configuration inputs | mitigate | Add regression tests proving malformed or missing kitchen config does not raise an unhandled exception out of `_merge_dags()`. |
| T-01-08 | Tampering | equipment constraint semantics | mitigate | Add a regression that proves scheduling constraints disappear when the required equipment is absent, so future scheduler changes cannot silently reintroduce phantom constraints. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add direct _merge_dags kitchen edge-case tests</name>
  <files>tests/test_kitchen_edge_cases.py</files>
  <read_first>tests/test_phase6_unit.py, app/graph/nodes/dag_merger.py, tests/fixtures/recipes.py, app/models/user.py</read_first>
  <action>Create `tests/test_kitchen_edge_cases.py` as a pure scheduler unit suite that mirrors the direct-call style from `tests/test_phase6_unit.py`. Add tests for these exact scenarios: `max_burners=0` with stovetop work results in no scheduled stovetop steps (or a structured degraded outcome) rather than a crash; missing/empty kitchen config falls back to defaults and still returns a merge result; malformed burner-descriptor input does not raise an unhandled exception out of `_merge_dags()`. Reuse fixture recipes or lightweight inline recipe builders instead of routing through LangGraph.</action>
  <verify>pytest -q tests/test_kitchen_edge_cases.py -o addopts=''</verify>
  <acceptance_criteria>
    - `tests/test_kitchen_edge_cases.py` contains `def test_zero_burners_does_not_schedule_stovetop_steps(`
    - `tests/test_kitchen_edge_cases.py` contains `def test_missing_kitchen_config_uses_defaults_without_crashing(`
    - `tests/test_kitchen_edge_cases.py` contains `def test_malformed_burner_descriptors_do_not_raise_unhandled_exception(`
  </acceptance_criteria>
  <done>The dedicated kitchen-edge suite exists and covers the three scheduler edge cases Phase 4 depends on.</done>
</task>

<task type="auto">
  <name>Task 2: Lock down equipment constraint unlock behavior</name>
  <files>tests/test_phase6_unit.py</files>
  <read_first>tests/test_phase6_unit.py, app/graph/nodes/dag_merger.py</read_first>
  <action>Add one targeted regression to `tests/test_phase6_unit.py` that proves equipment constraints vanish when the required equipment is missing from `kitchen_config`. Use two stovetop steps that would serialize when `equipment=['stand_mixer']` is present; assert they can overlap once the same kitchen config is rerun without `stand_mixer`. Keep the assertion focused on start times / overlap behavior instead of comparing the full merged schedule snapshot.</action>
  <verify>pytest -q tests/test_kitchen_edge_cases.py tests/test_phase6_unit.py -o addopts='' -k 'kitchen or equipment'</verify>
  <acceptance_criteria>
    - `tests/test_phase6_unit.py` contains a regression that compares with-equipment versus without-equipment scheduling behavior
    - the regression asserts overlap is allowed after the equipment constraint is removed
    - the targeted pytest command exercises both the new kitchen-edge suite and the updated scheduler regression
  </acceptance_criteria>
  <done>Equipment constraint unlock behavior is explicitly covered alongside the new kitchen-edge suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/test_kitchen_edge_cases.py tests/test_phase6_unit.py -o addopts='' -k 'kitchen or equipment'` passes
- [ ] edge-case tests assert graceful scheduler behavior instead of full snapshot equality
- [ ] the equipment unlock regression proves missing equipment removes the serialization constraint
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Kitchen edge cases are regression-covered for Phase 4
- Equipment constraint unlock behavior is locked down by tests

</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-04-SUMMARY.md`
</output>
