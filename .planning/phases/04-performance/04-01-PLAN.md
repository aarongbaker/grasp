---
phase: 04-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-performance/benchmark_dag_merger.py
  - .planning/phases/04-performance/04-PROFILE.md
  - app/graph/nodes/dag_merger.py
  - tests/test_kitchen_edge_cases.py
  - tests/test_phase6_unit.py
autonomous: true
requirements:
  - PERF-03
user_setup: []
must_haves:
  truths:
    - a repeatable profiling artifact captures `_merge_dags()` behavior for a realistic `10+` recipe / `50+` step workload before any scheduler change is made
    - if profiling shows `_IntervalIndex` already keeps slot-finding cost acceptable, Phase 4 closes as a docs-only proof with no production scheduler change
    - if profiling shows a remaining hotspot, only the measured hot path in `app/graph/nodes/dag_merger.py` changes and the scheduler regression gate remains green
  artifacts:
    - .planning/phases/04-performance/benchmark_dag_merger.py
    - .planning/phases/04-performance/04-PROFILE.md
    - .planning/phases/04-performance/04-01-SUMMARY.md
  key_links:
    - `.planning/phases/04-performance/benchmark_dag_merger.py` exercises the real `app/graph/nodes/dag_merger.py::_merge_dags()` path rather than an isolated interval helper
    - `tests/test_kitchen_edge_cases.py` and `tests/test_phase6_unit.py` are the locked regression gate for any scheduler decision in this phase
---

<objective>
Confirm whether scheduler slot-finding still needs optimization under realistic menu load, and only touch production scheduling code if the evidence proves it.

Purpose: Phase 4 exists to close the original scheduler performance concern without inventing unnecessary DAG-merger churn.
Output: a repeatable benchmark/profile artifact, a recorded performance decision, and either a docs-only closure or the smallest safe hot-path fix plus the scheduler regression gate.
</objective>

<execution_context>
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aaronbaker/Desktop/Projects/grasp/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance/04-CONTEXT.md
@.planning/phases/04-performance/04-RESEARCH.md
@.planning/phases/04-performance/04-VALIDATION.md
@app/graph/nodes/dag_merger.py
@tests/test_kitchen_edge_cases.py
@tests/test_phase6_unit.py
</context>

<threat_model>
## Trust Boundaries

- Scheduler latency is a denial-of-service concern when menus grow large enough to hit the slot-finding path repeatedly.
- Any optimization inside `dag_merger.py` risks changing the schedule contract if it is broader than the measured hot path.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `_merge_dags()` slot-finding path | mitigate | Capture a repeatable benchmark and cProfile run on a realistic `10+` recipe / `50+` step workload before deciding whether a fix is necessary. |
| T-04-02 | Tampering | scheduler output contract during optimization | mitigate | If a hotspot remains, confine changes to the measured hot path and rerun `tests/test_kitchen_edge_cases.py` plus `tests/test_phase6_unit.py` before closing the phase. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a repeatable scheduler benchmark and capture the Phase 4 profile</name>
  <files>.planning/phases/04-performance/benchmark_dag_merger.py, .planning/phases/04-performance/04-PROFILE.md</files>
  <read_first>app/graph/nodes/dag_merger.py, .planning/phases/04-performance/04-CONTEXT.md, .planning/phases/04-performance/04-RESEARCH.md, tests/test_kitchen_edge_cases.py, tests/test_phase6_unit.py</read_first>
  <action>Create `.planning/phases/04-performance/benchmark_dag_merger.py` as a phase-local helper that builds a realistic `_merge_dags()` workload with at least `12` recipes and `60` total steps, exercises stovetop and oven placement, prints a scaling table for `4`, `8`, `12`, and `16` recipes, and prints the top cumulative cProfile frames for the `12` recipe workload. Run it and write `.planning/phases/04-performance/04-PROFILE.md` with the exact command, the scaling results, the top profile frames, and an explicit `Decision:` section stating whether the current `_IntervalIndex` path already keeps the scheduler adequate.</action>
  <verify>./.venv/bin/python .planning/phases/04-performance/benchmark_dag_merger.py</verify>
  <acceptance_criteria>
    - `.planning/phases/04-performance/benchmark_dag_merger.py` exists
    - `.planning/phases/04-performance/benchmark_dag_merger.py` contains `for recipe_count in (4, 8, 12, 16)`
    - `.planning/phases/04-performance/04-PROFILE.md` contains `12 recipes / 60 steps`
    - `.planning/phases/04-performance/04-PROFILE.md` contains `Decision:`
  </acceptance_criteria>
  <done>The phase now has a repeatable benchmark artifact and a recorded evidence-based decision point.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase with either a docs-only proof or a narrow hot-path fix</name>
  <files>app/graph/nodes/dag_merger.py, tests/test_kitchen_edge_cases.py, tests/test_phase6_unit.py, .planning/phases/04-performance/04-01-SUMMARY.md</files>
  <read_first>.planning/phases/04-performance/04-PROFILE.md, app/graph/nodes/dag_merger.py, tests/test_kitchen_edge_cases.py, tests/test_phase6_unit.py, .planning/ROADMAP.md</read_first>
  <action>If `04-PROFILE.md` shows the current scheduler is already adequately fast and does not exhibit the originally suspected slot-finding blow-up, leave production scheduling code unchanged and close Phase 4 as a docs-only proof. If the profile still shows a clear hot path inside `app/graph/nodes/dag_merger.py`, apply the smallest safe fix to that measured seam only, keep scheduler outputs unchanged, and extend scheduler tests only if the hot-path change needs direct coverage. In either outcome, run `./.venv/bin/python -m pytest -q tests/test_kitchen_edge_cases.py tests/test_phase6_unit.py -o addopts=''` and write `.planning/phases/04-performance/04-01-SUMMARY.md` describing the benchmark evidence, the code-change decision, and the regression result.</action>
  <verify>./.venv/bin/python -m pytest -q tests/test_kitchen_edge_cases.py tests/test_phase6_unit.py -o addopts=''</verify>
  <acceptance_criteria>
    - `.planning/phases/04-performance/04-01-SUMMARY.md` contains `docs-only proof` or `narrow hot-path fix`
    - `.planning/phases/04-performance/04-01-SUMMARY.md` contains `tests/test_kitchen_edge_cases.py`
    - `.planning/phases/04-performance/04-01-SUMMARY.md` contains `tests/test_phase6_unit.py`
    - if `app/graph/nodes/dag_merger.py` changes, the summary names the exact hot path that was modified
  </acceptance_criteria>
  <done>Phase 4 is closed with a documented decision and the scheduler regression suite green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./.venv/bin/python .planning/phases/04-performance/benchmark_dag_merger.py` passes
- [ ] `./.venv/bin/python -m pytest -q tests/test_kitchen_edge_cases.py tests/test_phase6_unit.py -o addopts=''` passes
- [ ] `.planning/phases/04-performance/04-PROFILE.md` records the benchmark command and decision
- [ ] Phase 4 closes as either a docs-only proof or a measured narrow hot-path fix
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- scheduler performance is measured on a realistic workload before any code change decision
- the final phase decision is recorded as either `docs-only proof` or `narrow hot-path fix`
- the locked scheduler regression gate stays green

</success_criteria>

<output>
After completion, create `.planning/phases/04-performance/04-01-SUMMARY.md`
</output>
