# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── ORM / Database ────────────────────────────────────────────────────────────
sqlmodel==0.0.19
asyncpg==0.29.0                 # async Postgres driver for SQLAlchemy
psycopg[binary]==3.1.19         # psycopg3 — required by LangGraph PostgresSaver
psycopg-pool==3.2.2             # connection pool for LangGraph checkpointer

# ── Pydantic ──────────────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.1

# ── LangGraph / LangChain ─────────────────────────────────────────────────────
langgraph==0.2.28
langgraph-checkpoint-postgres==1.0.4   # PostgresSaver — separate install, not bundled
langchain==0.2.6
langchain-anthropic==0.1.19
langchain-openai==0.1.14

# ── LLM Providers ─────────────────────────────────────────────────────────────
anthropic==0.29.0
openai==1.35.3

# ── Vector Store ──────────────────────────────────────────────────────────────
pinecone==4.1.0                 # pinecone v4 client — upsert API changed from v2

# ── Graph / Scheduling ────────────────────────────────────────────────────────
networkx==3.3                   # DAG construction and cycle detection

# ── PDF / OCR ─────────────────────────────────────────────────────────────────
pymupdf==1.24.5                 # PDF rasterisation — fitz. 300 DPI page images.
Pillow==10.3.0                  # image handling for Vision pipeline

# ── Task Queue ────────────────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.7
kombu==5.3.7                    # Celery's transport layer — pin to avoid compat issues

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
httpx==0.27.0                   # async test client for FastAPI

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
python-multipart==0.0.9         # FastAPI file upload support
