# ── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── ORM / Database ────────────────────────────────────────────────────────────
alembic==1.15.2                 # database migrations
sqlmodel==0.0.19
asyncpg==0.29.0                 # async Postgres driver for SQLAlchemy
psycopg[binary]==3.3.3          # psycopg3 — required by LangGraph PostgresSaver (>=3.2 for checkpoint-postgres)
psycopg-pool==3.2.2             # connection pool for LangGraph checkpointer

# ── Pydantic ──────────────────────────────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.1

# ── LangGraph / LangChain ─────────────────────────────────────────────────────
langgraph==1.0.10
langgraph-checkpoint-postgres==3.0.4   # PostgresSaver — separate install, not bundled
langchain==1.2.10
langchain-anthropic==1.3.4
langchain-openai==1.1.10

# ── LLM Providers ─────────────────────────────────────────────────────────────
anthropic==0.84.0
openai==2.26.0

# ── Vector Store ──────────────────────────────────────────────────────────────
pinecone==4.1.0                 # pinecone v4 client — upsert API changed from v2

# ── Graph / Scheduling ────────────────────────────────────────────────────────
networkx==3.3                   # DAG construction and cycle detection

# ── PDF / OCR ─────────────────────────────────────────────────────────────────
pymupdf==1.24.5                 # PDF rasterisation — fitz. 300 DPI page images.
Pillow==10.3.0                  # image handling for Vision pipeline
pyobjc-framework-Vision==11.0; sys_platform == "darwin"   # Apple Vision OCR — macOS only
pyobjc-framework-Quartz==11.0; sys_platform == "darwin"   # CGImage support for Vision pipeline

# ── Task Queue ────────────────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.7
kombu==5.3.7                    # Celery's transport layer — pin to avoid compat issues

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
httpx==0.27.0                   # async test client for FastAPI

# ── Dev UI ───────────────────────────────────────────────────────────────────
streamlit==1.55.0               # test UI — not required for production

# ── Auth / Security ─────────────────────────────────────────────────────────
PyJWT==2.9.0                    # JWT token encoding/decoding
bcrypt==5.0.0                   # password hashing
slowapi==0.1.9                  # per-route rate limiting

# ── Observability ────────────────────────────────────────────────────────────
structlog==24.4.0               # structured logging (JSON in prod, pretty in dev)
tenacity==9.0.0                 # retry with exponential backoff for LLM calls

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
python-multipart==0.0.9         # FastAPI file upload support
